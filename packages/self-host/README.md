# Self Host

This is the self-host configuration distribution project for Secret.

It generates the configuration files needed by the CLI, Portal, and Edge packages from a user's deployment settings.
